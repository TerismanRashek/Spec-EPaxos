This folder contains the exact code that the CommitWithRecoverySpecFolder contains, except that the first operations are forced to get a certain history (see next relation)

This method is attempted in order to get more interesting results given that the full spec fails to run with 3 processes and 2 commands.